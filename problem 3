background(255);
colorMode(HSB);
noStroke();

while(true) {
  background(0, 0, 255, 0.1);
  fill(random(100,150), 255, 255);
  var i = random(0,2*PI);
  ellipse(width/2+100*cos(i), height/2+100*sin(i), random(25, 50));
}
